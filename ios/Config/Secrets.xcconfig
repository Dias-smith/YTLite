// Secrets — copy to Secrets.xcconfig.local and fill values (gitignored).
// These defaults allow the project to open; real values required for auth/sync.
SUPABASE_URL =
SUPABASE_ANON_KEY =
GOOGLE_CLIENT_ID =
YOUTUBE_DATA_API_KEY =
